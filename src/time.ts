export function dateLocal(){const parts=new Intl.DateTimeFormat('sv-SE',{timeZone:'Europe/Tallinn',year:'numeric',month:'2-digit',day:'2-digit',hour:'2-digit',minute:'2-digit',hourCycle:'h23'}).format(new Date());return parts.replace(' ','T');}
export function tallinnISO(value:string){const [d,t]=value.split('T');if(!d||!t)throw new Error('Vali kuupäev ja kellaaeg.');let ts=Date.parse(value+'Z');for(let i=0;i<3;i++){const repr=new Intl.DateTimeFormat('sv-SE',{timeZone:'Europe/Tallinn',year:'numeric',month:'2-digit',day:'2-digit',hour:'2-digit',minute:'2-digit',second:'2-digit',hourCycle:'h23'}).format(new Date(ts));ts+=Date.parse(value+'Z')-Date.parse(repr.replace(' ','T')+'Z');}return new Date(ts).toISOString();}
export function localFromISO(s:string){return new Intl.DateTimeFormat('sv-SE',{timeZone:'Europe/Tallinn',year:'numeric',month:'2-digit',day:'2-digit',hour:'2-digit',minute:'2-digit',hourCycle:'h23'}).format(new Date(s)).replace(' ','T');}
